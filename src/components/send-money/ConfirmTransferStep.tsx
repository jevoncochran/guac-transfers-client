import Typography from "@mui/material/Typography";

const ConfirmTransferStep = () => {
  return (
    <div>
      <Typography variant="transferStepHeading">Confirm and Send</Typography>
    </div>
  );
};

export default ConfirmTransferStep;
